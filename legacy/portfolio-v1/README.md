# Portfolio v1 (archived)

This folder contains the previous CodeHermit portfolio exactly as it existed before the 2026 rebuild.

It is intentionally excluded from the root Netlify build. To run it locally:

```bash
cd legacy/portfolio-v1
npm install
npm run dev
```

The active portfolio is the Astro project at the repository root.
